import test from "node:test"; import assert from "node:assert/strict";
import { ContextManager } from "../../src/context-manager.js"; import { TransactionRouter } from "../../src/transaction-router.js"; import { PromptRegistry } from "../../src/prompt-registry.js"; import { KnowledgeRegistry } from "../../src/knowledge-registry.js"; import { GovernanceLayer } from "../../src/governance-layer.js"; import { Observability } from "../../src/observability.js";
import { GP001Module } from "../../src/modules/gp001/gp001-module.js"; import { GP002Module } from "../../src/modules/gp002/gp002-module.js"; import { GP003Module } from "../../src/modules/gp003/gp003-module.js"; import { GP004Module } from "../../src/modules/gp004/gp004-module.js"; import { GP005Module } from "../../src/modules/gp005/gp005-module.js"; import { GP006Module } from "../../src/modules/gp006/gp006-module.js"; import { GP007Module } from "../../src/modules/gp007/gp007-module.js"; import { GP008Module } from "../../src/modules/gp008/gp008-module.js"; import { GP009Module } from "../../src/modules/gp009/gp009-module.js"; import { GP010Module } from "../../src/modules/gp010/gp010-module.js"; import { GP011Module } from "../../src/modules/gp011/gp011-module.js";
function runtime() { const contextManager = new ContextManager(); const core = { contextManager, router: new TransactionRouter(contextManager), promptRegistry: new PromptRegistry(contextManager), knowledgeRegistry: new KnowledgeRegistry(contextManager), governance: new GovernanceLayer(contextManager), observability: new Observability(contextManager) }; const gp001 = new GP001Module(core); const gp002 = new GP002Module(core); const gp003 = new GP003Module(core); const gp004 = new GP004Module({ ...core, lawCopilot: gp002, procurementCopilot: gp003 }); const gp005 = new GP005Module({ ...core, lawCopilot: gp002, procurementCopilot: gp003, financeCopilot: gp004 }); const gp006 = new GP006Module({ ...core, lawCopilot: gp002, financeCopilot: gp004, budgetCopilot: gp005 }); const gp007 = new GP007Module({ ...core, lawCopilot: gp002, budgetCopilot: gp005, hrCopilot: gp006 }); const gp008 = new GP008Module({ ...core, lawCopilot: gp002, procurementCopilot: gp003, financeCopilot: gp004, budgetCopilot: gp005, councilCopilot: gp007 }); const gp009 = new GP009Module({ ...core, lawCopilot: gp002, procurementCopilot: gp003, financeCopilot: gp004, budgetCopilot: gp005, councilCopilot: gp007, projectCopilot: gp008 }); const gp010 = new GP010Module({ ...core, gp001, gp002, gp003, gp004, gp005, gp006, gp007, gp008, gp009 }); const modules = { gp001, gp002, gp003, gp004, gp005, gp006, gp007, gp008, gp009, gp010 }; return { ...core, ...modules, gp011: new GP011Module({ ...core, ...modules }) }; }
const base = (overrides = {}) => ({ processing: { id: "P1", purpose: "public service", lawfulBasis: "legal-obligation", controller: "Municipality", dataMinimized: true, securityControls: ["encryption"], breachContact: "DPO" }, asOfDate: "2026-01-10", records: [{ citizen: "1234567890123", email: "a@example.com" }], retentionRules: [{ category: "service", periodDays: 365, legalBasis: "law", disposalMethod: "secure deletion" }], approvals: ["data-protection-officer"], template: "compliance-report", outputFormat: "json", principal: { id: "dpo", role: "data-protection-officer" }, ...overrides });
test("registers prompt, workflow, policies, templates, and knowledge idempotently", () => { const r = runtime(); new GP011Module(r); assert.equal(r.promptRegistry.getPrompt("GP011", "2.0.0").metadata.owner, "government-privacy-team"); assert.equal(r.knowledgeRegistry.search("", {}).filter(({ id }) => id.startsWith("gp011-")).length, 6); });
test("executes deterministic PDPA workflow through Sprint 1 Core", async () => { const r = runtime(); const a = await r.gp011.execute(base()); const b = await r.gp011.execute(base()); assert.equal(a.status, "completed"); assert.deepEqual(a.analysis, b.analysis); assert.equal(r.contextManager.getContext().activeModule, "GP011"); assert.ok(r.observability.getAuditEvents().some(({ type }) => type === "gp011.completed")); });
test("integrates GP001-GP010 interfaces and enforces approval workflow", async () => { const r = runtime(); const completed = { execute: async () => ({ status: "completed" }) }; const deps = { contextManager: r.contextManager, router: r.router, promptRegistry: r.promptRegistry, knowledgeRegistry: r.knowledgeRegistry, governance: r.governance, observability: r.observability, ...Object.fromEntries(Array.from({ length: 10 }, (_, i) => [`gp${String(i + 1).padStart(3, "0")}`, completed])) }; const module = new GP011Module(deps); const linkedReviews = Object.fromEntries(Array.from({ length: 10 }, (_, i) => [`gp${String(i + 1).padStart(3, "0")}`, {}])); const linked = await module.execute(base({ linkedReviews })); assert.equal(Object.keys(linked.analysis.crossModule).length, 10); const gated = await module.execute(base({ approvals: [] })); assert.equal(gated.status, "approval_required"); });
test("supports Markdown, HTML, JSON, API, and audit outputs", async () => { const r = runtime(); for (const format of ["markdown", "html", "json", "api-response", "audit-log"]) { const result = await r.gp011.execute(base({ outputFormat: format })); assert.equal(result.status, "completed"); assert.ok(result.output); } });
