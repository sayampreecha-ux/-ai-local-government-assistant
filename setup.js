# GovPrompt Thailand Enterprise 4.2 — Safe Launch Edition

## จุดเด่น

- เพิ่ม `SALES_ENABLED` เป็นสวิตช์ป้องกันการรับคำสั่งซื้อก่อนระบบพร้อม
- เพิ่มหน้า `/setup.html` สำหรับตรวจฐานข้อมูล การชำระเงิน โหมด AI และสถานะเปิดขาย
- Health API แยกสถานะ “API ทำงาน” ออกจาก “พร้อมขายจริง”
- API แพ็กเกจมีข้อมูลสำรอง ทำให้หน้าตัวอย่างแสดงผลได้แม้ยังไม่เชื่อม Supabase
- หน้าหลักแสดงแถบสถานะและปิดปุ่มซื้ออัตโนมัติเมื่อยังไม่เปิดขาย
- เพิ่ม Public Config API โดยไม่เปิดเผยค่ารหัสลับ

## หลักการเปิดขาย

ตั้ง `SALES_ENABLED=false` ระหว่างติดตั้งและทดสอบ เมื่อหน้า `/setup.html` แสดงว่าฐานข้อมูลและข้อมูลรับชำระเงินพร้อมแล้ว จึงเปลี่ยนเป็น `true` และ Redeploy
