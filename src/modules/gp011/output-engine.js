import { GP011_TEMPLATE_LABELS } from "./templates/index.js";
const escapeHtml = (value) => String(value).replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;");
function markdown(result) { return [`# ${GP011_TEMPLATE_LABELS[result.template]}`, `## Processing\n${result.report.processingId} — ${result.report.purpose}`, `## Detection\nDetected: ${result.detection.detected}; Types: ${result.detection.types.join(", ") || "none"}`, `## Risk\n${result.risk.level} (${result.risk.score}/100)`, `## Controls\n${result.checklist.passed}/${result.checklist.total}`, `## Approval\n${result.approval.ready ? "ready" : `missing: ${result.approval.missing.join(", ")}`}`, `## Status\n${result.report.status}`].join("\n\n"); }
function html(result) { return `<!doctype html><html lang="th"><head><meta charset="utf-8"><title>${escapeHtml(GP011_TEMPLATE_LABELS[result.template])}</title></head><body><main><h1>${escapeHtml(GP011_TEMPLATE_LABELS[result.template])}</h1><h2>Processing</h2><p>${escapeHtml(result.report.processingId)} — ${escapeHtml(result.report.purpose)}</p><h2>Risk</h2><p>${escapeHtml(result.risk.level)} (${result.risk.score}/100)</p><h2>Status</h2><p>${escapeHtml(result.report.status)}</p></main></body></html>`; }
export function formatGP011Output(result, format, audit = {}) { if (format === "json") return structuredClone(result); if (format === "markdown") return markdown(result); if (format === "html") return html(result); if (format === "audit-log") return { event: "gp011.compliance-report", audit: structuredClone(audit), result: structuredClone(result) }; if (format === "api-response") return { ok: true, moduleId: "GP011", data: structuredClone(result), meta: structuredClone(audit) }; throw new RangeError(`Unknown GP011 output format: ${format}`); }
