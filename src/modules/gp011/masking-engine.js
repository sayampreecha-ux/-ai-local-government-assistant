const MASKS = Object.freeze([[/\b\d{13}\b/g, (v) => `${v.slice(0, 1)}***********${v.slice(-1)}`], [/\b[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}\b/gi, (v) => `${v[0]}***@${v.split("@")[1]}`], [/\b0\d{8,9}\b/g, (v) => `${v.slice(0, 2)}*****${v.slice(-3)}`]]);
export function maskValue(value) { if (typeof value === "string") return MASKS.reduce((result, [regex, replacement]) => result.replace(new RegExp(regex.source, regex.flags), replacement), value); if (Array.isArray(value)) return value.map(maskValue); if (value && typeof value === "object") return Object.fromEntries(Object.entries(value).map(([key, nested]) => [key, maskValue(nested)])); return value; }
export function autoMask(records, detection) { return { applied: detection.detected, records: maskValue(records), maskedTypes: detection.types.filter((type) => ["national-id", "email", "phone"].includes(type)), unresolvedSensitive: detection.types.filter((type) => ["health", "biometric"].includes(type)) }; }
