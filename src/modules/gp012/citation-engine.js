export function buildCitations(results) { const citations = results.map((item) => ({ citationId: `C${String(item.rank).padStart(3, "0")}`, documentId: item.id, title: item.title, authority: item.authority, version: item.version, effectiveDate: item.effectiveDate, reference: item.reference, valid: Boolean(item.title && item.authority && item.version && item.reference) })); return { citations, valid: citations.every(({ valid }) => valid), count: citations.length }; }
