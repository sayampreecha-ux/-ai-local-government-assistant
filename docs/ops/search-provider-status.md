# External search provider status

The production Worker can deploy successfully even when the external search provider is temporarily unavailable. Production security checks remain blocking. Live search quality remains visible as an advisory check so upstream provider incidents do not mask deployment health.
